"use client";
import React from 'react';

function Calendar() { // displays calendar in form of day, week, month, year (displays whatever is there on the database which is nothing rn)
    // takes either "day", "week", "month", "year" as props to display respective view, default is week
    // takes whatever day is today as prop to display that week/month/year/day (always start from that week's sunday)
    
    return <div>Calendar Component</div>;
}
export default Calendar;